services:
    _defaults:
        autowire: false
        autoconfigure: false
        public: false

    cm.hasher.user_password:
        class: CongregationManager\Bundle\User\Hasher\UserPasswordHasher
        arguments:
            - '@security.user_password_hasher'
