parameters:
    app_admin.path_name: 'admin'
    default_locale: 'en'
    supported_locales: en|it

services:
    _defaults:
        autowire: false
        autoconfigure: false
        public: false

