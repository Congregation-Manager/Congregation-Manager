<h1>{{ 'cm.email.reset_password.hello'|trans_app }}</h1>

<p>{{ 'cm.email.reset_password.intro'|trans_app }}</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>{{ 'cm.email.reset_password.expire'|trans_app({'{expireIn}': resetToken.expirationMessageKey|trans_app(resetToken.expirationMessageData, 'ResetPasswordBundle') }) }}</p>

<p>{{ 'cm.email.reset_password.greetings'|trans_app }}</p>
