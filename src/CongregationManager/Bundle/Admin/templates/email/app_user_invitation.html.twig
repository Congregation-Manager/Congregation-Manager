<h1>{{ 'congregation_manager_admin.email.app_user_invitation.hello'|trans_admin({}, localeCode) }}</h1>

<p>{{ 'congregation_manager_admin.email.app_user_invitation.intro'|trans_admin({}, localeCode) }}</p>

{% set url = url('congregation_manager_app_complete_account', {token: invitationToken}) %}
<a href="{{ url }}">{{ url }}</a>

<p>{{ 'congregation_manager_admin.email.app_user_invitation.expire'|trans_admin({'{expireIn}': 2 }, localeCode) }}</p>

<p>{{ 'congregation_manager_admin.email.app_user_invitation.greetings'|trans_admin({}, localeCode) }}</p>
