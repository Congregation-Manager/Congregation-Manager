<h1>{{ 'app.email.reset_password.hello'|trans }}</h1>

<p>{{ 'app.email.reset_password.intro'|trans }}</p>

<a href="{{ url('admin_reset_password', {token: resetToken.token}) }}">{{ url('admin_reset_password', {token: resetToken.token}) }}</a>

<p>{{ 'app.email.reset_password.expire'|trans({'{expireIn}': resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }) }}</p>

<p>{{ 'app.email.reset_password.greetings'|trans }}</p>
